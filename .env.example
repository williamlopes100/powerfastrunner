# ============================================================
# Power Fast Runner - Environment Variables (TEMPLATE)
# Copy this file to .env and fill in real values.
# Never commit .env to version control.
# ============================================================

# Required. Port the Express server binds to (no hardcoded default).
PORT=3000
